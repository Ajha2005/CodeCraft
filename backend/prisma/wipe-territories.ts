import 'dotenv/config'; import { PrismaClient } from '../generated/prisma/client'; import { PrismaPg } from '@prisma/adapter-pg'; const adapter = new PrismaPg({ connectionString: process.env.DATABASE_URL }); const prisma = new PrismaClient({ adapter }); async function main() { const owned = await prisma.territoryOwnership.deleteMany({}); console.log(`Deleted ${owned.count} ownership rows`); const territories = await prisma.territory.deleteMany({}); console.log(`Deleted ${territories.count} territory rows`); } main().finally(() => prisma.$disconnect());